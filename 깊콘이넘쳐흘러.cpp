// https://www.acmicpc.net/problem/17420
#include <iostream>
#include <utility>
#include <vector>

using namespace std;

int main() {
    int N = 0;
    cin >> N;
    vector<int> A(N), B(N);
    for (int &_ : A)
        cin >> _;
    for (int &_ : B)
        cin >> _;
    vector<pair<int, int>> AB(N);
    for(auto&[a,b]:AB){
        
    }

    return 0;
}